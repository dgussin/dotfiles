- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.zprofile:

- create:
    - ~/Work

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
